import Title from '@/components/ui/Title'

export default function Benefit() {
  return (
		<section>
			<Title title='Выгоды для заказчика' className='max-w-[245px] sm:max-w-max' />
		</section>
	)
}